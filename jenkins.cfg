[buildout]
extends =
    devel-4.2.cfg

eggs-directory = ${config:cache_dir}/eggs
download-cache = ${config:cache_dir}/downloads
extends-cache = ${config:cache_dir}/extends


[config]
cache_dir = /tmp
